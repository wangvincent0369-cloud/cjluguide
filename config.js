/* ============================================================
   config.js —— 站点配置（开源版唯一必须改的文件）
   ------------------------------------------------------------
   谁要改成自己的站：先动这个文件，再替换各页面的内容。
   联系方式留空 = 界面上完全不出现（默认如此，防爬虫收割）。
   本文件被 contact.js / search.js / analytics.js 读取，
   引入顺序必须在它们之前（每个页面 </body> 前的第一个 <script>）。
   ============================================================ */
window.SITE = {
  /* 站名 / 署名：显示在联系方式卡顶部的小字 */
  name: '新生指南（开源演示版）',

  /* 站长微信号：留空 = 搜索「无结果」出口和联系卡都不出现微信号。
     想公开就填，但注意：静态站+开源的代码会被爬虫收割，填了就等于公开。 */
  wx: '',

  /* 群二维码文件名（如 'your-qr.jpg'，与页面同目录）：
     留空 = 每页底部的联系方式卡显示占位说明。 */
  qrFile: '',

  /* 行为统计（可选）：开源版默认关闭。
     on 改 true 后，访问/搜索/点击会向 endpoint 发匿名计数请求；
     保持 false = 全程零网络请求。 */
  stats: {
    on: false,
    endpoint: 'https://your-counter.example/hit/your-site/'
  }
};
